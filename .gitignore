# ── Secrets & credentials (NEVER commit) ──
.env
*.env
.env.*

# ── FAISS binary indexes (large, regenerate from DB) ──
*.faiss
*.pkl

# ── Python cache ──
__pycache__/
*.pyc
*.pyo
*.pyd

# ── Node / frontend build ──
node_modules/
dist/
.vite/

# ── ALL bat and ps1 scripts ──
*.bat
*.ps1

# ── Scripts subfolders (research/debug/migration/tools — local only) ──
scripts/

# ── Result text files ──
*_result.txt
*.txt

# ── Data / snapshots (local) ──
data/
snapshots/
train_images/

# ── SQLite fallback ──
*.db
*.sqlite

# ── OS files ──
.DS_Store
Thumbs.db

# ── Misc ──
d
